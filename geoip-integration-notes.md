# E聊 GeoIP 接入记录

## 方案比较

| Approach                                | Tradeoffs                                                                      |                                                 Cost | Setup Complexity |
| --------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------: | ---------------: |
| HTTPS GeoIP 服务 + 进程缓存（本次采用） | 无需部署数据库，可立即返回国家/省州/城市；依赖第三方网络，需超时与降级         | 默认服务可低量使用；生产规模应购买商业套餐或更换配置 |               低 |
| MaxMind GeoLite2 本地数据库             | 无逐请求外部依赖，适合高并发；需要注册、许可证密钥、下载数据库并在 30 天内更新 |             GeoLite 数据免费；维护更新任务有运维成本 |             中高 |

## 外部来源

- [ipwho.is](https://ipwho.is/) 的 HTTPS 响应包含 `success`、`country`、`country_code`、`region`、`city`、`connection.isp/asn` 等字段。本次仅保存国家、省州、城市和来源标识，不保存经纬度。
- [IP-API 文档](https://ip-api.com/docs/api:json)说明免费端点不支持 HTTPS、限制每 IP 每分钟 45 次且不允许商业使用，因此不作为默认生产接入。
- [MaxMind GeoLite2 官方文档](https://dev.maxmind.com/geoip/geolite2-free-geolocation-data/)说明需注册并生成许可证密钥；Web 服务限制每天 1000 次，数据库适合高频查询但必须保持更新，且 IP 定位本质上并不精确，不能用于识别具体住址或家庭。

## 本次实现原则

1. 默认通过可配置 HTTPS URL 查询真实公网 IP 地区；使用 6 秒超时、24 小时成功缓存和 5 分钟失败缓存，以覆盖当前环境约 4–5 秒的首次 DNS/TLS/服务响应，同时限制第三方故障对登录的影响。
2. 本机、内网、链路本地、保留地址不发起外部请求。
3. 第三方失败、超时或返回无效数据时降级为“公网 IP · x.x.x.x”，绝不阻断注册、登录、退出或后台操作。
4. 登录成功时把真实地区写入用户 `LastLoginAddress`；登录、离线、管理员审计和报错日志也写入同一 GeoIP 结果。
5. 不保存纬度、经度、邮编等非必要精细位置；管理界面显示的地址属于 IP 推断区域，不代表精确位置。
