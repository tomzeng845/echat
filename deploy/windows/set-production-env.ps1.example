# Copy this file to C:\Program Files\EChat\echat.env.ps1 and edit it as Administrator.
# Do not commit the edited copy or send it by email/chat.

$env:MONGODB_URI = "mongodb://root:Heibai%40996666@10.63.125.3:27018/admin?connectTimeoutMS=3000000&timeoutMS=50000&maxIdleTimeMS=600000&authMechanism=SCRAM-SHA-1&directConnection=true"
$env:MONGODB_DATABASE = "echat"
$env:MONGODB_DIRECT_CONNECTION = "true"
$env:JWT_SECRET = "replace-with-a-long-random-secret"
$env:ADMIN_BOOTSTRAP_PASSWORD = "replace-before-first-start"
$env:ADMIN_SECRET_ENCRYPTION_KEY = "replace-with-a-long-random-secret"
$env:ADMIN_TOTP_SECRET = "replace-with-a-base32-totp-secret"
$env:SEED_INVITE_CODE = "replace-with-the-first-invite-code"
$env:CORS_ALLOWED_ORIGINS = "https://chat.example.com"
# Optional: enable direct Kestrel HTTPS instead of HTTP.
# The PFX file must be readable by the EChat Windows service account.
# $env:ECHAT_HTTPS_CERT_PATH = "C:\Program Files\EChat\certs\platform.superseller88.com.pfx"
# $env:ECHAT_HTTPS_CERT_PASSWORD = "replace-with-pfx-password"
# $env:ECHAT_HTTPS_PORT = "2099"

# Optional features:
# $env:TURN_URLS = "turn:turn.example.com:3478"
# $env:TURN_SECRET = "replace-with-turn-secret"
# $env:SFU_URL = "https://sfu.example.com"
# $env:FCM_PROJECT_ID = "your-firebase-project-id"
# $env:FCM_SERVICE_ACCOUNT_JSON = '{"type":"service_account"}'

# iOS APNs production push. Keep the .p8 file outside the release ZIP and
# restrict its ACL to the EChat service account and Administrators.
# $env:APNS_TEAM_ID = "PPY8H6QWB5"
# $env:APNS_KEY_ID = "35Z9TC62QQ"
# $env:APNS_BUNDLE_ID = "com.tomzeng845.echat"
# $env:APNS_USE_SANDBOX = "false"
# $env:APNS_PRIVATE_KEY = (Get-Content "C:\Program Files\EChat\secrets\AuthKey_35Z9TC62QQ.p8" -Raw)

# Import this file in the current PowerShell session before a manual start:
# . "C:\Program Files\EChat\echat.env.ps1"
# & "C:\Program Files\EChat\EChat.Api.exe"
