# E聊后台需求文档 V2 对照记录

**来源：** `/home/ubuntu/upload/pasted_file_cR9Oac_后台管理的功能需求对接-2.docx`（7 页）

## 明确变更

1. 用户管理不得显示 Admin、Operator、Reviewer 等后台账号。
2. “修改邀请码”弹窗改为只读用户账号、现有邀请码列表、8 位新邀请码和随机生成按钮，并连接更新 API。
3. 修复 CSV 导出；分页栏移到用户列表底部。
4. 登录日志改为密集筛选表格；“客户端/版本”改为“地址”，地址由登录 IP 映射。
5. 意见反馈改为密集表格，增加“只看未查看”、单条查看、批量标记已查看和分页。
6. 额度科目方向只保留“收入、支出”。额度增减记录下新增“交易明细”菜单，提供分页和专用 API。
7. 新增管理账号时，角色下拉必须来自角色管理数据，而不是硬编码枚举。
8. 角色管理的权限范围改为菜单功能权限下拉选择。
9. 公告管理改为列表、筛选、新增/编辑、发布/撤回、删除和分页；新增弹窗使用文档表单。
10. 操作日志增加“地址”；报错日志增加设备类型、设备型号、操作系统版本、APP 版本号。
11. 会话管理与群管理改为文档中的高密度列表、查询、状态和操作布局，连接专用 API。
12. 群发言列表和新增弹窗字段包括群名称、抢账号、群管理/机器人发关键字账号、抢自动回复内容、开始/结束时间、启用状态、发言开始/结束间隔；增加关键字设置和替换发言内容弹窗。
13. 删除通讯录模块和菜单。
14. 机器人发信息列表与新增弹窗字段包括账号、内容、发送顺序、是否启用。
15. 抢红包机器人列表与新增弹窗字段包括群名称、账号、开始/结束时间、随机时间、是否启用；真实资金行为仍必须保持禁用边界。

## 截图视觉模式

所有模块采用浅色高密度运营表格：顶部操作按钮、同一行筛选项、分页记录统计、彩色状态标签和每行操作按钮。弹窗为白底分组表单，左侧固定标签、右侧输入或下拉，底部“关闭/提交”。用户邀请码弹窗明确展示现有邀请码区域；群发言、机器人和抢红包弹窗均使用群聊/账号下拉并显示启用状态。

## 安全与实现边界

IP 地址映射将在 .NET API 内提供确定性地址字段，并允许后续接入 GeoIP 数据库；不能依赖外部服务时不得伪造精确城市。安卓厂商推送、机器人消息和抢红包配置必须区分“配置已保存”与“真实外部执行成功”。抢红包机器人当前只允许配置与审计，不执行资金动作。会话管理只能审计服务端持有的密文元数据，不绕过端到端加密读取明文。

## 0.7.0 实现结果

| 文档模块           | 最终实现                                                               | API / 数据边界                                                      |
| ------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 用户管理           | 只显示普通用户；账号、认证、状态、IP、风险和余额密集表格；分页位于底部 | `/api/admin/users` 在服务端排除所有非 User 角色                     |
| 修改邀请码         | 操作弹窗加载可用八位邀请码并保存                                       | `GET invite-options`、`PUT invite-code`                             |
| 登录/离线/失败日志 | 支持筛选、分页与处置，展示设备类型、型号、系统、版本、IP 和地址        | 地址由请求 IP 分类；未接入第三方地理库时显示本机、内网或公网 IP     |
| 意见反馈           | 详情、回复、拒绝、删除、单条/批量标记已查看、底部分页                  | 批量状态写入 `account.feedback` 并记录审计                          |
| 额度与交易         | 科目方向、金额范围、启停、幂等增减、记录和独立交易明细                 | 金额使用 invariant decimal；余额更新与台账同一 API 流程             |
| 管理账号           | 只允许 Admin，支持创建、编辑、停用/删除与账号独立 TOTP                 | 低权限角色不进入该页面；当前管理员不可自我停用/删除                 |
| 角色管理           | 固定权限列表多选、筛选、编辑、删除                                     | API 白名单拒绝未知权限                                              |
| 公告与图片         | 公告 CRUD/发布/撤回；图片上传、分类、标签、搜索和删除                  | 公告发布实时推送；删除图片库记录不会伪称已删除外部对象字节          |
| 操作/报错日志      | 增加地址、设备、系统和版本字段，支持搜索与分页                         | 所有关键后台变更写管理员审计                                        |
| 会话与群管理       | 服务端筛选分页、成员/群主信息、密文时间线、后台建群、改名、解散与恢复  | 建群临时生成 AES 群密钥，仅保存成员 RSA-OAEP 信封并立即清零明文密钥 |
| 客服/群发言/机器人 | 新增、筛选、分页、编辑、删除和人工触发                                 | 人工触发通过 SignalR 通知并写发送日志；红包机器人不执行真实资金动作 |
| 群邀请码           | 群选择、次数、有效期、查询、撤销和用户兑换                             | 与注册邀请码完全隔离                                                |
| 删除页面           | 厂商推送、通讯录及上一版已删除项目不再显示                             | 通讯录管理 API 返回 410；已删除通用模块不在编辑白名单               |

## 验收结果

`pnpm check`、Vitest 3 项、xUnit 15 项和生产构建通过。业务冒烟测试返回 `E2E_OK`；文档专用 API 验收返回 `ADMIN_REQUIREMENTS_070_OK`；管理端 API 与浏览器验收返回 `ADMIN_070_OK modules=7 pages=24`。桌面 1440×900 与手机 390×844 截图复核通过。
